//
//  Mocka
//

import UniformTypeIdentifiers

extension UTType {
  /// A `UTType` with identifier `public.css`.
  static let css = UTType("public.css")

  /// A `UTType` with identifier `public.comma-separated-values-text`.
  static let csv = UTType("public.comma-separated-values-text")

  /// A `UTType` with identifier `public.folder`.
  static let folder = UTType("public.folder")

  /// A `UTType` with identifier `public.html`.
  static let html = UTType("public.html")

  /// A `UTType` with identifier `public.json`.
  static let json = UTType("public.json")

  /// A `UTType` with identifier `public.plain-text`.
  static let plainText = UTType("public.plain-text")

  /// A `UTType` with identifier `public.xml`.
  static let xml = UTType("public.xml")
}
