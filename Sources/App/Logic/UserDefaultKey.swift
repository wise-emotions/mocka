//
//  Mocka
//

import Foundation

/// The `Key`s used to save and fetch values from `UserDefaults`.
enum userDefaultKey {
  /// The root path that should be used when fetching and saving the requests and responses created by the user.
  static let rootPath = "rootPath"
}
